"use client";
import { useState } from "react";
import { PageHeading } from "./PostInternshipPage";

/** Direct/group messaging interface with a functioning composer. */
export function MessagesPage(){const[active,setActive]=useState("Dr. Samuel Ofori");const[draft,setDraft]=useState("");const[messages,setMessages]=useState<string[]>([]);const contacts=["Dr. Samuel Ofori","Esi Asare","Ashanti Region Group","CSTSI Support"];return <><PageHeading label="COMMUNICATION" title="Messages" copy="Direct and regional conversations with internship stakeholders."/><div className="module-card messaging-layout"><aside>{contacts.map(name=><button className={active===name?"active":""} onClick={()=>setActive(name)} key={name}><b>{name.split(" ").map(x=>x[0]).slice(0,2).join("")}</b><span><strong>{name}</strong><small>Open conversation</small></span></button>)}</aside><section><header><strong>{active}</strong><span>Active now</span></header><div className="message-thread"><p className="received">Please remember the scheduled visit at 10:00 AM.</p><p className="sent">Thank you. My lesson note is ready.</p>{messages.map((m,i)=><p className="sent" key={i}>{m}</p>)}</div><form onSubmit={e=>{e.preventDefault();if(!draft.trim())return;setMessages([...messages,draft]);setDraft("")}}><input value={draft} onChange={e=>setDraft(e.target.value)} placeholder="Write a message..."/><button className="primary">Send</button></form></section></div></>}
