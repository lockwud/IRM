"use client";
import Link from "next/link";
import { useState } from "react";

export default function ForgotPasswordPage(){const[message,setMessage]=useState("");const[resetUrl,setResetUrl]=useState("");async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();const data=new FormData(event.currentTarget);const response=await fetch("/api/auth/forgot-password",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({identifier:data.get("identifier")})});const result=await response.json();setMessage(result.message||result.error);setResetUrl(result.resetUrl||"")}return <main className="auth-simple"><form onSubmit={submit}><div className="auth-logo"><img src="/ustedlogo.jpeg" alt="AAMUSTED logo"/></div><span className="auth-kicker">ACCOUNT RECOVERY</span><h1>Reset your password</h1><p>Enter the student email, staff ID or coordinator email associated with your account.</p>{message&&<div className="auth-success">{message}</div>}<label>Email or staff ID<input required name="identifier" placeholder="Enter your account identifier"/></label><button className="auth-submit">Send reset instructions</button>{resetUrl&&<Link className="development-reset" href={resetUrl}>Open development reset link →</Link>}<Link className="back-login" href="/login">← Back to login</Link></form></main>}
